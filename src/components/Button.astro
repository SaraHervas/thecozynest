---
const { href, class: className = '' } = Astro.props;
---
<a
  href={href}
  class={`inline-block bg-mustard hover:bg-mustard-dark transition-colors text-cream font-semibold px-7 py-3 rounded-sm ${className}`}
>
  <slot />
</a>
